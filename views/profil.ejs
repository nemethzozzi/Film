<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Házi Mozi</title>
    <link rel="stylesheet" href="/css/alap_style.css">
    <link rel="stylesheet" href="/css/profil_bejelentkezes.css">
    <link rel="icon" href="img/popcorn.png">
</head>
<%
    //if(typeof user !== undefined && user !== null){
        sessionStorage.setItem("id", user[felhasznaloId]);
        sessionStorage.setItem("email",user[email]);
        sessionStorage.setItem("nev", user[nev]);
        sessionStorage.setItem("tipus", user[felhasznaloTipus]);
        console.log("atadott nev: " + user[nev] + "sessionStorage nev: " + sessionStorage.getItem("nev"));
        alert("Bejelentkeztél mint: $1", user[nev]);
    //}
%>
<body>
    <header>

        <a href="/"><span>Házi Mozi</span></a>
        <span> Profil </span>
        <!-- Profil -->
        <div class="dropdown">
            <img src="img/user.png" alt="user">
            <div class="dropdown-content">
                <a href="/profil">Profil</a>
                <a href="/login">Bejelentkezés</a>
                <a href="/regisztracio">Regisztráció</a>
            </div>
        </div>
        <!-- Szures -->
        <a href="/szures"><img src="img/search.png" alt="search"></a>
        <!-- Megnezendok -->
        <a id="eye" href="/megnezendo"><img src="img/szem.png" alt="szem"></a>
        <a id="eye_closed" href="/megnezendo"><img src="img/csukva.png" alt="csukott_szem"></a>

        <script>
            document.getElementById("eye_closed").hidden = true;
            document.getElementById("eye").onmouseenter = function() {

                document.getElementById("eye").hidden = true;
                document.getElementById("eye_closed").hidden = false;
            }
            document.getElementById("eye_closed").onmouseleave = function (){
                document.getElementById("eye").hidden = false;
                document.getElementById("eye_closed").hidden = true;
            }
        </script>

    </header>
    
    <!--Profil-->
    <!--<img class="user" src="img/profile.png" alt="user" width="100px" height="100px">-->
    
    <h1 class="feher">Profil</h1>
    <form  id="formok_formazasa">
        <label>Felhasználónév:  <input type="text" id="name" name="name" placeholder=""><br></label>

        <label>Email cím:  <input type="email" id="email" name="email" placeholder=""><br></label>

        <label>Jelszó:  <input type="password" id="password" autocomplete="off" name="password" placeholder=""><br></label>

        <label>Születési dátum:  <input type="date" name="birthdate"><br></label>

        <input type="submit" value="Mentés" name="save">
        
        <label><input type="checkbox" checked="checked" name="hirlevel">Hírlevél feliratkozás</label>
    </form>
    
    <script>
        var nev = sessionStorage.getItem("nev");
        var email = sessionStorage.getItem("email");
        var jelszo = "123Kuytus!";
        document.getElementById("name").placeholder = nev;
        document.getElementById("email").placeholder = email;
        document.getElementById("password").placeholder = jelszo;
        </script>
</body>
</html>
